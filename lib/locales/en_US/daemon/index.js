'use strict'
/* eslint-disable no-template-curly-in-string */
module.exports = {
  closeEventTip: 'System requirements receive exit signals[type:%s]',
  run: {
    repeat: 'Daemon thread is repeated start',
    succss: 'Daemon threads started successfully',
    fail: 'Daemon thread failed to start'
  },
  server: {
    api: 'The daemon thread is in the outbound RPC-API service startup'
  }
}

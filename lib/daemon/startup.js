'use strict'
const debug = require('debug')('api/local')
// ddv路径模块
const Api = module.exports = {
  startupInstall () {
    debug('startupInstall')
  },
  startupUninstall () {
    debug('startupInstall')
  }
}
Api
